services:
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env.staging
    volumes:
      - wedding-staging-data:/data
    environment:
      - DB_PATH=/data/wedding-staging.db

volumes:
  wedding-staging-data:
